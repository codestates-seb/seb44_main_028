import { PreImageWrapper } from '../style';
const PreImage = ({ imageSrc }) => {
  return (
    <PreImageWrapper>
      <img src={ImageSrc} />
    </PreImageWrapper>
  );
};

export default PreImage;
